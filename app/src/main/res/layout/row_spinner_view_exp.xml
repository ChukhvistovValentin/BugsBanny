<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:filterTouchesWhenObscured="false"
    android:orientation="horizontal"
    android:background="@drawable/back_month_diagram"
    android:theme="@android:style/Theme">

    <ImageView
        android:id="@+id/icon_spinner"
        android:layout_width="45dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/other"
        android:padding="5dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/spiner_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/choose_date_end"
        android:textSize="22dp"
        android:gravity="center_vertical"
        android:paddingLeft="5dp"
        android:textColor="@android:color/black"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/icon_spinner"
        android:layout_toEndOf="@+id/icon_spinner"
        android:textStyle="bold" />

</RelativeLayout>